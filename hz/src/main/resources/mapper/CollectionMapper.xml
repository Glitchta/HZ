<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.hz.mapper.CollectionMapper">

    <select id="selectByUserIdAndType" resultType="com.study.hz.pojo.Collection">
        SELECT * FROM collection
        WHERE user_id = #{userId}
        <if test="contentType != null and contentType != ''">
            AND content_type = #{contentType}
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countByUserIdAndType" resultType="int">
        SELECT count(*) FROM collection
        WHERE user_id = #{userId}
        <if test="contentType != null and contentType != ''">
            AND content_type = #{contentType}
        </if>
    </select>

    <select id="selectByUserAndContent" resultType="com.study.hz.pojo.Collection">
        SELECT * FROM collection
        WHERE user_id = #{userId} AND content_id = #{contentId} AND content_type = #{contentType}
    </select>

</mapper>
