<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.hz.mapper.CommentMapper">
    <resultMap id="CommentResultMap" type="com.study.hz.dto.CommentDto">
        <id property="id" column="id"/>
        <result property="createUser" column="create_user"/>
        <result property="contentId" column="content_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="replyId" column="reply_id"/>
        <result property="content" column="content"/>
        <result property="images" column="images"
                typeHandler="com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler"/>
        <result property="commentCount" column="comment_count"/>
        <result property="likeCount" column="like_count"/>
        <result property="createTime" column="create_time"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
    </resultMap>

    <select id="selectCommentList" resultMap="CommentResultMap" parameterType="com.study.hz.dto.PageDto">
        select c.*, u.nickname, u.avatar from comment c
        left join user_info u on c.create_user = u.user_id
        where c.content_id = #{pageDto.id} and c.parent_id is null
        order by c.create_time desc
        limit #{pageDto.offset},#{pageDto.size}
    </select>

    <delete id="deleteByParentId">
        delete from comment where parent_id = #{parentId}
    </delete>

    <select id="selectReplies" resultMap="CommentResultMap">
        select c.*, u.nickname, u.avatar from comment c
        left join user_info u on c.create_user = u.user_id
        where c.parent_id = #{parentId}
        order by c.create_time asc
        limit #{offset},#{size}
    </select>

</mapper>
